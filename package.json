{
  "name": "grokdev",
  "version": "0.1.0",
  "private": true,
  "description": "grokdev — autonomous memecoin developer streaming 24/7, brain on Grok (xAI)",
  "scripts": {
    "build": "npm --prefix server install --omit=dev",
    "start": "node server/index.js"
  },
  "engines": { "node": ">=20" }
}
